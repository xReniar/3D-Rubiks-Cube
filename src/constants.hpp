#ifndef CONSTANTS_H
#define CONSTANTS_H

enum Direction {
    CLOCKWISE,
    ANTICLOCKWISE
};

enum _Side_ {
    TOP_SIDE,
    BOTTOM_SIDE,
    FRONT_SIDE,
    RIGHT_SIDE,
    BACK_SIDE,
    LEFT_SIDE
};

enum LayerType {
    INNER,
    OUTER
};

enum Color { 
    YELLOW,
    WHITE,
    BLUE,
    RED,
    GREEN,
    ORANGE,
    BLACK
};

#endif